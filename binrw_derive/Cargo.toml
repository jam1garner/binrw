[package]
name = "binrw_derive"
version = "1.4.0"
authors = ["jam1garner <jam1.mcleod@hotmail.com>"]
edition = "2018"
description = "Derive macro for binread"
repository = "https://github.com/jam1garner/binrw"
license = "MIT"

[lib]
proc-macro = true

[dependencies]
syn = { version = "1", features = ["extra-traits", "full"] }
quote = "1"
proc-macro2 = "1"

[dev-dependencies]
binrw = { version = "1.3", path = "../binrw" }

[features]
debug_template = []
